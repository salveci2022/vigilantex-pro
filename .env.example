# =====================================================
# VIGILANTEX PRO — Variáveis de Ambiente
# Copie este arquivo para .env e preencha os valores
# =====================================================

# Flask
SECRET_KEY=vigilantex-chave-secreta-2026-troque-isso

# Banco de dados PostgreSQL (Render)
DATABASE_URL=postgresql://usuario:senha@host:5432/vigilantex

# Z-API WhatsApp (preencha no painel admin após subir)
# Configure via painel de administração do sistema
ZAPI_DEFAULT_INSTANCE=
ZAPI_DEFAULT_TOKEN=

# Ambiente
FLASK_ENV=production
PORT=5000
